apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-matheus.fullname" . }}
  labels:
    {{- include "hello-matheus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-matheus.selectorLabels" . | nindent 4 }}
