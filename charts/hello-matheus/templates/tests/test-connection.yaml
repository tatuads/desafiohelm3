apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-matheus.fullname" . }}-test-connection"
  labels:
    {{- include "hello-matheus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-matheus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
